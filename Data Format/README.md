The files 'X_train_info.csv' and 'X_valid_info' contain info about the causal
pairs, there are also three columns:
• the identifier of the pair
• the type for variable A (Numerical, Categorical, Binary)
• the type for
